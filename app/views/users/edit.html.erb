<%= form_with(model: @user) do |form| %>
    <%= form.collection_check_boxes :role_ids, Role.all, :id, :name %>
    <%= @user.errors[:roles].join(", ") if @user.errors[:roles].any? %>
    <%= form.button :submit %>
  <% end %>
  